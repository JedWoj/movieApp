import { createSlice, PayloadAction } from '@reduxjs/toolkit';

const serialsSlice = createSlice({
    name: "serials",
    initialState: {
        
    },
    extraReducers: (builder) => {
        
    },
    reducers: {
        
    }
});

export const userActions = serialsSlice.actions;
export default serialsSlice;